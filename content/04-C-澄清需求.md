# 第四章：C - Clarify（澄清需求）

## 4.1 引言

**本章目标**：学会用 AI 把一个模糊想法变成一份完整的需求文档。

Clarify 是 CODE 的第一步，也是最容易被跳过的一步。

很多人拿到任务后第一反应是"赶紧写代码"——有了 AI 之后更甚，因为 AI 让"马上出代码"变得太容易了。但没想清楚就动手，结果就是反复返工。

**Clarify 的目标很明确：在写任何代码之前，产出一份清晰的需求文档。** 这份文档不是给领导看的汇报材料，而是给你自己和 AI 看的"施工图纸"——后续的架构设计、编码、测试都以它为依据。

本章运用第一、二章学过的技巧（CRC 三要素 + 苏格拉底式提问），不再重复讲解方法本身，重点放在**需求文档的结构**和**完整的实战演示**上。

---

## 4.2 什么是好的需求文档

一份好的需求文档应该包含四个部分：

### 1. 功能需求：做什么

功能需求回答"系统需要提供哪些能力"。可以用**功能点列表**或**用户故事**来描述。

**功能点列表风格：**
```
- 用户可以创建待办事项（包含标题、描述、优先级、截止日期）
- 用户可以查看自己的待办事项列表，支持分页和按优先级筛选
- 用户可以修改待办事项的内容和状态
- 用户可以删除待办事项
```

**用户故事风格：**
```
- 作为用户，我希望能创建待办事项并设定优先级，以便管理日常任务
- 作为用户，我希望能按优先级筛选待办事项，以便快速找到重要任务
```

两种风格都可以，关键是**具体、可验证**。

### 2. 非功能需求：做到什么程度

非功能需求容易被遗忘，但往往决定了技术方案的选择。

| 类型 | 说明 | 示例 |
|------|------|------|
| 性能 | 响应时间、吞吐量 | "列表查询接口响应时间 < 200ms" |
| 安全 | 认证、授权、数据保护 | "用户只能看到自己的待办事项" |
| 可用性 | 系统稳定性要求 | "服务可用性 99.9%" |
| 可维护性 | 代码和架构的可维护要求 | "模块化设计，便于后续扩展" |

### 3. 边界条件：什么情况不处理

明确**不做什么**，和明确**做什么**同样重要。

```
- 本期不做：团队协作（多人共享待办事项）
- 本期不做：文件附件上传
- 本期不做：移动端适配
- 限制：单个用户最多 1000 条待办事项
```

边界条件能有效防止需求蔓延，也让 AI 知道哪些场景不需要考虑。

### 4. 验收标准：怎么算做完了

验收标准是**可检验的条件列表**。做完后逐条对照，全部通过就算完成。

```
- [ ] 用户可以注册、登录，获取 JWT Token
- [ ] 使用 Token 可以创建待办事项，不带 Token 返回 401
- [ ] 创建时 title 为空返回 400，错误信息清晰
- [ ] 列表查询支持分页，默认每页 20 条
- [ ] 用户只能查看和操作自己的待办事项
```

### 好需求 vs 差需求

| 差需求 | 好需求 |
|--------|--------|
| "做一个 Todo 应用" | "做一个支持 CRUD、分类、优先级的 Todo API" |
| "要快" | "列表查询响应时间 < 200ms" |
| "要安全" | "JWT 认证 + 用户数据隔离" |
| "做完就行" | 有明确的验收标准清单 |

**差需求的共同特点：模糊、不可验证。好需求的共同特点：具体、可检验。**

---

## 4.3 Clarify 的流程

三步走，简单直接：

```mermaid
graph LR
    A["第一步<br/>CRC 初稿"] --> B["第二步<br/>AI 提问查漏"]
    B --> C["第三步<br/>生成需求文档"]
```

**第一步：CRC 初稿**
- 用 CRC 写出你目前已知的信息（参考第一章）
- 不需要完美，能说清楚大概要做什么就行

**第二步：AI 提问查漏**
- 让 AI 用苏格拉底式提问帮你发现遗漏（参考第二章）
- 建议使用"一次一问"模式，逐步深入
- 重点关注：非功能需求、边界条件、异常场景

**第三步：生成需求文档**
- 对话结束后，让 AI 将讨论结论整理成结构化的需求文档
- 按照"功能需求 + 非功能需求 + 边界条件 + 验收标准"的结构输出

---

## 4.4 实战演示：Todo API 需求澄清

以下展示一个完整的 Clarify 过程。

### 第一步：CRC 初稿

```
## Context
我要开发一个待办事项管理系统，后端用 Python，前端用 Vue。
目前是个人项目，后续可能给团队使用。

## Requirement
实现一个 Todo API，支持基本的增删改查功能。

## Constraint
先帮我澄清需求。不要写代码，先向我提问，一次一个问题，
确保需求完整。当你认为信息足够时告诉我。
```

### 第二步：AI 提问查漏（对话过程）

> **AI**：Todo 的数据字段有哪些？除了标题，还需要描述、优先级、截止日期、标签等字段吗？
>
> **你**：需要标题、描述、优先级（高中低）、截止日期。不需要标签。
>
> **AI**：用户体系方面，是否需要用户注册和登录？每个用户只能管理自己的待办事项？
>
> **你**：需要注册登录，用户数据要隔离。
>
> **AI**：待办事项是否有状态流转？比如：待办 → 进行中 → 已完成？还是只有"未完成"和"已完成"两种状态？
>
> **你**：就两种：未完成和已完成。
>
> **AI**：列表查询方面，是否需要支持按优先级筛选、按状态筛选？是否需要分页？
>
> **你**：都需要，分页默认 20 条。
>
> **AI**：非功能需求方面，对性能和并发有要求吗？比如预期的用户量和请求量？
>
> **你**：初期几十个用户，不用考虑高并发。
>
> **AI**：最后一个问题——是否需要"软删除"（标记删除但数据保留）？还是直接从数据库删除？
>
> **你**：软删除。
>
> **AI**：信息已经足够了，我来整理需求文档。

注意 AI 问出了几个你可能没想到的问题：状态流转设计、软删除策略。这些如果到了编码阶段才发现，就要返工。

### 第三步：AI 生成的需求文档

```markdown
# Todo API 需求文档

## 1. 功能需求

### 1.1 用户管理
- 用户注册（邮箱 + 密码）
- 用户登录（返回 JWT Token）
- 用户数据隔离（只能操作自己的数据）

### 1.2 待办事项 CRUD
- 创建待办事项：title（必填）、description（可选）、
  priority（high/medium/low，默认 medium）、due_date（可选）
- 查询待办事项列表：支持分页（默认 20 条/页）、
  按优先级筛选、按状态筛选
- 修改待办事项：可修改所有字段及完成状态
- 删除待办事项：软删除（标记 is_deleted，不物理删除）

### 1.3 状态管理
- 两种状态：未完成（pending）、已完成（completed）
- 支持标记完成和取消完成

## 2. 非功能需求
- 性能：初期用户量小（< 100），无高并发要求
- 安全：JWT Token 认证，密码加密存储
- 数据：软删除，数据可恢复

## 3. 边界条件
- 不做：团队协作、多人共享
- 不做：文件附件上传
- 不做：标签/分类管理（本期）
- 不做：移动端适配
- 前端需求另行澄清（本文档聚焦 API 设计）

## 4. 验收标准
- [ ] 用户可以注册、登录，获取 JWT Token
- [ ] 未登录访问接口返回 401
- [ ] 可以创建待办事项，title 为空时返回 400
- [ ] 列表查询支持分页，返回总数和当前页数据
- [ ] 可以按优先级和状态筛选
- [ ] 可以修改待办事项内容和状态
- [ ] 删除为软删除，已删除项不出现在列表中
- [ ] 用户只能操作自己的待办事项，操作他人数据返回 403
- [ ] 密码不以明文存储（使用哈希加密）
- [ ] API 响应时间 < 500ms
```

审查确认后，将这份文档保存为 `docs/requirements.md`。后续的架构设计（O 阶段）和开发实现（D 阶段）都会通过文件引用（如 `@docs/requirements.md`）把它作为上下文输入。

---

## 4.5 需求文档模板

以下模板可以直接复用。让 AI 生成需求文档时，把这个模板作为 Constraint 传给它：

```
请按照以下结构整理需求文档：

## 1. 功能需求
按模块列出所有功能点，每个功能点要具体、可验证。

## 2. 非功能需求
包括性能、安全、可用性、可维护性等方面的要求。

## 3. 边界条件
明确列出本期不做的功能和不处理的场景。

## 4. 验收标准
用 checkbox 列出所有验收条件，每条都可以客观判断通过与否。
```

---

## 4.6 常见误区

### 误区 1：需求写太细，变成技术方案

```
❌ "用 PostgreSQL 的 JSONB 字段存储 Todo 的扩展属性，
    建立 B-tree 索引加速查询"
```

这是技术方案，不是需求。需求应该是"支持待办事项的扩展属性"——怎么实现是 O 阶段（架构设计）的事。

**原则：需求描述"做什么"，不描述"怎么做"。**

### 误区 2：只写功能需求，忘了非功能需求

"能用就行"的心态，会导致上线后出各种问题：性能差、安全漏洞、数据丢失。

非功能需求不需要写得很复杂，但至少要**有意识地想过**：性能够不够？安全怎么保障？数据会不会丢？

### 误区 3：不写边界条件，导致需求蔓延

没有明确"不做什么"，就会不断有人问"能不能加个 XX 功能？"。一个简单的 Todo API 最后变成了项目管理系统。

**边界条件是保护你的围栏。**

---

## 4.7 练习题

### 练习：完成一次 Clarify

选择以下任一场景（或用你自己的真实任务），完成完整的 Clarify 流程：

**场景 A**：开发一个"用户反馈收集"接口（提交反馈、查看反馈列表、标记已处理）

**场景 B**：开发一个"API 访问日志记录"模块（记录请求信息、查询日志、统计接口调用次数）

要求：
1. 写出 CRC 初稿
2. 让 AI 提问，至少 5 轮对话
3. 让 AI 生成最终的需求文档（四个部分齐全）
4. 检查需求文档：功能需求是否具体？非功能需求有没有遗漏？边界条件清楚吗？验收标准可检验吗？

---

## 4.8 本章小结

**核心要点回顾：**

- Clarify 的目标是**产出一份完整的需求文档**，不是写代码
- 需求文档四部分：功能需求、非功能需求、边界条件、验收标准
- 流程三步走：CRC 初稿 → AI 提问查漏 → 生成需求文档
- 需求描述"做什么"，不描述"怎么做"
- 边界条件和验收标准同样重要，不能只写功能

**下一章预告：**

需求清楚了，下一步是想好怎么实现。第五章进入 **O - Outline（架构设计）**，学习用 BASER 五要素系统地进行架构设计。
