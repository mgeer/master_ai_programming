# 全书 Review 问题与建议

## 一、P0 — 必须修复（影响读者跟练）

### 1. Refresh Token 跨章节不一致

**现状**：

- 第 4 章 Constitution（4.9 节）：安全策略中写了 "Refresh Token 有效期：7 天"
- 第 5 章 Clarify（5.7 节）：明确决策 "本期不做 Refresh Token"
- 第 6 章 Plan（6.7 节 API 契约、6.9 节认证方案）：包含 Refresh Token 的完整设计（流程图、API 端点、刷新逻辑）
- 第 7 章 Tasks：Task 13 是 "Token 刷新"

存在两层矛盾：

1. Constitution 说有 Refresh Token，Clarify 说不做——项目级规范与功能级决策矛盾
2. Plan 和 Tasks 按"做 Refresh Token"设计——与 Clarify 矛盾，但与 Constitution 一致

**建议**：统一为**做 Refresh Token**。理由：Constitution 和 Plan 都已包含，删除要改动更多地方；Refresh Token 也是 JWT 认证的完整实践，有教学价值。只需把第 5 章 Clarify 中"本期不做 Refresh Token"的决策删除或改为"支持简单的 Token 刷新机制"。

### 2. AC（验收标准）编号跨章节不一致

**现状**：

- 第 5 章产出的 AC 编号（如 AC-1 到 AC-N）与第 7 章引用的 AC 编号（如 AC-7、AC-27、AC-28、AC-33~35）对不上
- 部分 AC 编号在第 5 章中找不到对应条目，或内容描述与第 5 章原文不一致

**建议**：

1. 在第 5 章 Clarify 完成后，产出一份完整、连续编号的 AC 列表（AC-1 到 AC-N）
2. 第 6 章 Plan 和第 7 章 Tasks/Implement 中引用 AC 时，严格对照第 5 章的编号和内容
3. 建议在写作时维护一份独立的 AC 对照表，确保全书引用一致

---

## 二、P1 — 应该修复（影响读者上手体验）

### 3. 缺少"在哪操作"的指引

**现状**：从第 4 章开始，每个 SDD 阶段都给了提示词，但没说这些提示词应该在什么环境下使用——是在 Cursor Agent 模式？新建聊天？还是同一个对话连续推进？读者不知道每个阶段是该开新对话还是继续上一个对话。

**建议**：在第 4 章开头（或第 3 章末尾）加一段操作指引，例如：

> **实操建议**：
> - Constitution：在 Cursor 中新建一个 Agent 对话，专门完成项目宪章的构建。完成后保存为 `.spec/constitution.md`。
> - Specify + Clarify：可以在同一个对话中连续进行（它们是一对搭档），最终产出 `spec.md`。
> - Plan：建议开新对话，通过 `@spec.md` 和 `@constitution.md` 引用已有文件作为上下文。
> - Tasks：可以继续 Plan 的对话，也可以开新对话引用 `@plan.md`。
> - Implement：每个 Task 建议开新对话，引用 `@tasks.md`、`@plan.md` 和相关源码文件。
>
> 经验法则：当对话超过 15-20 轮时，建议开新对话并带上关键文件引用，避免上下文丢失。

### 4. 第 5 章 Clarify 阶段的文件更新操作不够具体

**现状**：5.7 节中，用户回答完 AI 的问题后说 "请根据我的回答更新功能规范"。但没有说明 AI 是直接修改文件还是在对话中输出更新后的全文。在 Cursor Agent 模式和普通聊天模式下，操作方式不同。

**建议**：在 5.7 节加一个操作提示：

> **操作提示**：如果你使用 Cursor Agent 模式，AI 可以直接修改 `spec.md` 文件。如果使用普通聊天模式，AI 会输出更新后的规范文本，你需要手动替换 `spec.md` 的内容。建议每次 Clarify 结束后，打开文件确认已正确更新。

### 5. 第 7 章 Task 1-7 的前置条件缺乏说明

**现状**：7.7 节开头说"假设 Task 1-7 已经完成"，但 Task 1-7 的实现代码在书中没有展示。读者跟练时会卡在这里——不知道是要先自己做完 Task 1-7，还是可以直接跳到 Task 8。

**建议**（任选一种）：

- **方案 A**：提供 GitHub 仓库，读者可以 checkout 到 Task 7 完成后的代码状态
- **方案 B**：在 7.7 节开头明确告诉读者操作路径：

> Task 1-7 的实现模式与下文演示的 Task 8 完全相同——都是 Test → Implement → Review 循环。建议你先通读本章了解完整模式，然后从 Task 1 开始按顺序实践。

### 6. 第 6 章 API 查询参数超出 Spec 范围

**现状**：第 6 章的 API 契约为 `GET /api/v1/todos` 设计了 `created_after`、`created_before`、`sort_by`、`sort_order` 等查询参数，但这些筛选需求在第 5 章的 `spec.md` 中没有定义。

**建议**：

- 在第 5 章 spec.md 中补充日期范围筛选和排序的需求描述与 AC
- 或者从第 6 章 Plan 中删除这些未经 Spec 定义的参数

两者择一，核心是保持 Spec → Plan 的可追溯性。

### 7. 第 6 章业务规则引用了未定义需求

**现状**：第 6 章 6.5 节业务规则中包含 "V-10: 筛选的结束日期不能早于开始日期"，但日期范围筛选需求在 spec.md 中不存在。

**建议**：与第 6 点一起处理——要么在 Spec 中增加日期筛选需求，要么删除这条业务规则。

---

## 三、P2 — 建议优化（提升阅读体验）

### 8. 第 4 章各要素的"讲"部分偏冗长

**现状**：第 4 章六个要素（架构原则、技术栈、安全策略等）的"讲"部分，用了较多篇幅解释"为什么重要"。对于目标读者（有经验的程序员），这些解释偏啰嗦。

**建议**：精简"讲"部分，聚焦于"这个要素在 **AI 编程**中为什么特别重要"——而不是一般性地解释为什么重要。例如编码规范的"讲"可以直接切入："没有编码规范，AI 今天用 `snake_case` 明天用 `camelCase`，代码风格一团糟"——一句话就够了。

### 9. 附录增加 SDD 流程速查表

**现状**：读者读完全书后，日常使用时需要翻回各章节找提示词和产出物。缺少一个快速参考页。

**建议**：在附录中增加一页"SDD 流程速查表"：

| 阶段 | 启动提示词要点 | 审查重点 | 产出物 |
|------|--------------|---------|--------|
| Constitution | "为这个项目建立项目宪章，包含架构、技术栈、规范、安全、部署" | 规矩之间有没有矛盾？有没有过度设计？ | `.spec/constitution.md` |
| Specify | "帮我写功能规范，包含用户故事、FR、NFR、AC" | 需求具体、可验证、无歧义？ | `.spec/features/xxx/spec.md` |
| Clarify | "逐条审查规范，找出模糊、遗漏、矛盾的地方" | 所有问题都回答了？AC 覆盖完整？ | 更新 `spec.md` |
| Plan | "基于 spec 和 constitution，做领域建模和技术设计" | 与 spec 一致？与 constitution 一致？ | `.spec/features/xxx/plan.md` |
| Tasks | "把 plan 拆解为可独立验证的任务列表" | 覆盖所有 FR/AC？任务间依赖清晰？ | `.spec/features/xxx/tasks.md` |
| Implement | "按第 N 个任务，先写测试再实现" | 测试通过？代码符合规范？ | `src/` + `tests/` |

### 10. 第 2 章对话管理补充 SDD 各阶段的 @ 引用策略

**现状**：2.8 节"对话管理"提到"善用工具的上下文能力"，但写得很泛。读者想知道 SDD 各阶段具体应该引用哪些文件。

**建议**：在 2.8 节"善用工具的上下文能力"部分加一个前瞻提示：

> 在后续的 SDD 实战中，你会频繁用到文件引用。一个简单的规则是：**每个阶段引用上一个阶段的产出物 + Constitution**。例如：
> - Plan 阶段：`@constitution.md` + `@spec.md`
> - Tasks 阶段：`@plan.md`
> - Implement 阶段：`@tasks.md` + `@plan.md` + 相关源码

### 11. 第 8 章内容密度偏低

**现状**：第 8 章作为"迭代实战"，主要是 Feature 2 的快速走马灯式展示（简要的 spec、plan、tasks 摘要），更像一份总结而不是实战。缺少"迭代决策"的演示——比如 Feature 2 的设计和 Feature 1 冲突时怎么处理。

**建议**：

- 增加一个"迭代决策点"的场景：比如标签功能的实现方式有多种选择（直接加字段 vs 关联表 vs 标签服务），展示用"方案对比"技巧做决策的过程
- 增加一个"跨 Feature 影响"的场景：比如给 Todo 加标签后，`GET /api/v1/todos` 的筛选参数需要扩展，展示如何回退修改 Plan

### 12. 全书缺少"AI 出错 → 诊断 → 修复"的场景

**现状**：书中所有 AI 产出示例都是高质量的。没有展示 AI 产出不可用代码、生成了错误的测试、或者幻觉了不存在的 API 时，读者该怎么应对。

**建议**：在第 7 章 Implement 阶段或第 9 章最佳实践中，增加一个小节"当 AI 出错时"，展示一个真实的错误场景和修复流程。例如：

> AI 生成的代码使用了 `from jose import jwt`，但你的 `requirements.txt` 中写的是 `PyJWT`（`import jwt`）。这种"库名混淆"是 AI 常见错误——它把 `python-jose` 和 `PyJWT` 搞混了。
>
> 诊断方法：看错误信息 → 检查 import 语句 → 对照依赖列表。
> 修复方法：告诉 AI "我们使用的是 PyJWT 库，不是 python-jose，请修正 import"。

---

## 四、P3 — 小修小补

### 13. 比喻重复

"盖房子"的比喻在第 3、5、7 章出现了至少 3 次。建议保留第 3 章的首次引入，后续章节简化引用（如"就像前面说的盖房子"）或换用新比喻。

### 14. 术语不统一：密码哈希 vs 密码加密

全书中"密码哈希"和"密码加密"混用。bcrypt 是哈希算法，不是加密算法。建议统一为"密码哈希"。

### 15. 术语不统一：状态码 400 vs 422

部分 AC 中对请求体校验失败使用了 400，部分使用了 422。FastAPI 默认对 Pydantic 校验失败返回 422。建议统一为 422 Unprocessable Entity（请求体校验失败）和 400 Bad Request（通用参数错误）。

### 16. 第 1 章编程案例的 Todo 数据结构与后续不一致

第 1 章 1.3 节的编程场景中，Todo 包含 `priority`（优先级）和 `due_date`（截止日期），但第 5 章开始的正式 spec 中没有这两个字段。

**建议**：在第 1 章该案例前加一句说明："以下是一个假设的 Todo 数据结构用于演示 CRC 写法，和后续章节的正式规范可能不同。" 或者将第 1 章示例改为与正式 spec 一致的字段。

### 17. ID 类型不一致：UUID vs int

第 6 章 Plan 中明确指定主键使用 UUID，但第 7 章的 Pydantic 模型代码示例中 `id` 的类型是 `int`。

**建议**：将第 7 章代码示例中的 `id: int` 改为 `id: UUID`，或在第 6 章 Plan 中改为使用自增整数 ID。两者择一，保持一致。

### 18. Markdown 代码块嵌套的转义方式

书中使用零宽空格（`​`）来转义嵌套的 Markdown 代码块。读者复制这些代码时，零宽空格可能被带入，导致执行出错。

**建议**：改用四个反引号（````）包裹外层代码块，或使用缩进来表示嵌套代码块，避免不可见字符。

### 19. GitHub Spec Kit 链接需确认

第 9 章和 README 中的 GitHub Spec Kit 链接可能不一致。建议确认官方链接后统一。

### 20. 全书结语的位置

全书结语（"最后说句掏心窝的话"）目前放在第 9 章末尾，而不是附录之后。

**建议**：将结语移到附录之后，作为全书的收尾。附录是参考资料，结语是作者的最后赠言——放在最后更有仪式感。

### 21. 练习题缺少参考答案

第 1、2 章的练习题没有提供参考答案或思路提示。

**建议**：至少提供每道练习题的"参考思路"或"示例答案要点"，帮助读者自查。可以放在每章末尾或附录中。
